# MCP 服务器配置
# 服务器监听地址
# 使用 localhost 只允许本机访问
# 使用 0.0.0.0 允许从任何IP访问（在Windows上更可靠）
MCP_SERVER_ADDRESS=0.0.0.0:12345

# API密钥，用于客户端和服务端认证
# 建议使用复杂随机字符串
API_KEY=your-secret-api-key

# MCP 客户端配置
# 服务器URL (客户端用)
# 注意：如果是本机访问，使用 localhost 或 127.0.0.1
# 如果是远程访问，使用服务器IP地址
MCP_SERVER_URL=http://127.0.0.1:12345/sse

# OpenAI配置 (客户端用)
# 需要有效的OpenAI API密钥
OPENAI_API_KEY=sk-25a1c25d80714b8dbd1b5feeead94b7a
# OpenAI API基础URL，可使用官方或代理
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# 使用的OpenAI模型
OPENAI_MODEL=qwen-max